"use client"; // Tambahkan "use client" jika Anda menggunakan komponen client-side

import React from "react";
import Complaint from "./components/complaint";

const ComplaintPage: React.FC = () => {
  return <Complaint />;
};

export default ComplaintPage;
